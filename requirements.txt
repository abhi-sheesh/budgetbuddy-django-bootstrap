install pip (for debian/ubuntu: sudo apt install python3-pip)

in command line:
1. python -m venv {name_of_virtual_env} [python -m venv venv]
2. activate venv [venv/Scripts/activate on windows powershell, Source venv/bin/activate on linux]

pip install django

pip install dotenv
pip install django-crispy-forms
pip install crispy-bootstrap5
pip install django-filter
pip install django-chartjs
pip install django-tables2
pip install django-widget-tweaks

pip install scikit-learn
pip install pandas
pip install statsmodel


python manage.py runserver (will run at port 8000 default but specify port if necessary like 8001)

install remaining necessary libraries